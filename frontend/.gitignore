.env
# .env.production KHÔNG bị ignore: VibeHost build image trực tiếp từ Git,
# Vite cần đọc VITE_API_URL từ file này ngay trong bước "npm run build"
# bên trong Docker — biến môi trường set ở dashboard chỉ áp dụng lúc
# container CHẠY (sau khi build), nên không kịp cho bước build tĩnh này.
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist_deploy.zip
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
